/*
 * ShowStrings Messages
 *
 * This contains all the text for the ShowStrings container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ShowStrings';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ShowStrings container!',
  },
});
